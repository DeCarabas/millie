# I can infer the type of factorial and apply it correctly.
#
# ExpectedType: int
let rec factorial =
    fn n =>
       if n = 0 then
           1
       else
           n * factorial (n + -1)
in
    factorial 5
